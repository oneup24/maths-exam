// ENGINE 3: Classifies student errors by type
// e.g. calculation error, conceptual error, trap item confusion

export function classifyError(question, studentAnswer, correctAnswer) {
  // TODO: implement error classification logic
  return { type: 'unknown', detail: '' };
}
